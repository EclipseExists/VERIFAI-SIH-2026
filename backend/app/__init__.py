# ---------------------------------------------------------------------------
# backend/app/__init__.py
# ---------------------------------------------------------------------------
# This file is intentionally left empty.
#
# Its sole purpose is to mark the `app` directory as a Python package so that
# Python's import system can resolve modules inside it.  For example:
#
#     from app.core.config import get_settings
#
# Without this __init__.py, Python would not recognise `app` as a package
# and the import above would fail with a ModuleNotFoundError.
# ---------------------------------------------------------------------------
