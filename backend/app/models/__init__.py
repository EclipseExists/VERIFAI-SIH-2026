# ---------------------------------------------------------------------------
# backend/app/models/__init__.py
# ---------------------------------------------------------------------------
# This file is intentionally left empty.
#
# It marks the `models` directory as a Python sub-package of `app`.
# The `models` package contains SQLAlchemy ORM models that map Python
# classes to PostgreSQL tables.  Each file in this package typically defines
# one domain entity (User, Case, Document, AuditLog, etc.).
# ---------------------------------------------------------------------------
