# ---------------------------------------------------------------------------
# backend/app/core/__init__.py
# ---------------------------------------------------------------------------
# This file is intentionally left empty.
#
# It marks the `core` directory as a Python sub-package of `app`.
# The `core` package holds cross-cutting concerns such as application
# configuration, security utilities, and shared constants that every other
# module in the project may depend on.
# ---------------------------------------------------------------------------
