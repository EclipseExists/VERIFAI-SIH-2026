# ---------------------------------------------------------------------------
# backend/app/db/__init__.py
# ---------------------------------------------------------------------------
# This file is intentionally left empty.
#
# It marks the `db` directory as a Python sub-package of `app`.
# The `db` package encapsulates everything related to database connectivity:
# engine creation, session management, and the dependency-injection helper
# that FastAPI route handlers use to obtain a database session.
# ---------------------------------------------------------------------------
