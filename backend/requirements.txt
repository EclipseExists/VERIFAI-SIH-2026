# VERIFAI Backend Dependencies
# ==============================
# Pin minimum versions, not exact versions, for hackathon flexibility.
# Run: pip install -r requirements.txt

# ── Web Framework ─────────────────────────────────────────────────
fastapi>=0.115.0          # The web framework — handles HTTP routing, validation, docs
uvicorn[standard]>=0.30.0 # ASGI server — actually runs the FastAPI app

# ── Database ──────────────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0  # ORM — maps Python classes to database tables
asyncpg>=0.29.0             # Async PostgreSQL driver (fastest available)
alembic>=1.13.0             # Database migration tool (for schema changes)

# ── Data Validation ───────────────────────────────────────────────
pydantic>=2.0.0             # Data validation — ensures API inputs are correct
pydantic-settings>=2.0.0    # Reads config from .env files
email-validator>=2.0.0      # Validates email format in user registration

# ── Authentication ────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0  # JWT token creation and verification
passlib[bcrypt]>=1.7.4            # Password hashing (bcrypt)

# ── File Upload ───────────────────────────────────────────────────
python-multipart>=0.0.9    # Required by FastAPI for file upload endpoints

# ── Testing ───────────────────────────────────────────────────────
pytest>=8.0.0              # Test framework
pytest-asyncio>=0.23.0     # Async test support (our endpoints are async)
httpx>=0.27.0              # HTTP client for testing FastAPI without a server

# ── AI Module Dependencies ────────────────────────────────────────
# These are needed by the ai/ teammate modules (ocr, mrz, face, forensics)
opencv-python-headless>=4.8.0   # Image processing (headless = no GUI needed on server)
numpy>=1.24.0                   # Math/array operations for all AI modules
Pillow>=10.0.0                  # Image manipulation (forensics ELA + noise)
paddlepaddle>=2.5.0             # PaddleOCR backend engine
paddleocr>=2.7.0                # OCR text extraction from document images
deepface>=0.0.89                # Face verification (used by ai/face/verify.py)
tf-keras                        # DeepFace needs this as a backend
